# Unitree G1 Docs

- Use this directory as the local reference corpus for Unitree G1 Edu behavior, APIs, services, setup, and examples.
- Start with `index.md` to find relevant topics, then read the matching files in `pages/`. Use `raw/` only when checking the original mirrored source.
- Treat documentation as evidence, not proof of current robot state. Call out when an answer is based on docs, inference, or observed behavior on PC2.
- For robot-facing commands or code, assume execution on PC2 unless the user explicitly says the task is Mac-side.
- If updating docs is necessary, keep edits small and traceable; avoid broad formatting churn or filename/path changes.
